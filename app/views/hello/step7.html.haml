= render 'shared/main_header'
= render 'share/breadcrumbs'
.mypage
  = render 'users/mypage-left'
  .logout-main
  - if user_signed_in?
    = link_to destroy_user_session_path, method: :delete do
      .logout-main__registration-btn
        %p ログアウト
= render 'shared/main_footer'